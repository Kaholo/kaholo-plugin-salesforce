{
	"name": "kaholo-plugin-salesforce",
	"version": "1.0.0",
	"description": "Kaholo plugin for integration with SalesForce API.",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kaholo Group",
	"license": "ISC",
	"dependencies": {
		"jsforce": "^1.10.1"
	}
}